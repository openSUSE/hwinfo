mk_floppy
=========

Create a hardware test floppy; call form the libhd dir after doing
a 'make'. Don't forget to make a 'cmu' before!


cmu
===

Helper tool needed on the test floppy; it replaces cat, sleep, mount, umount.

